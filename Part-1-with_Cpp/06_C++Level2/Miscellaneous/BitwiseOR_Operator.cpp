#include <iostream>
using namespace std;

// it convert the numbers to binary and then combine them using: OR operation 
//<The Power with "1" it's like (+) Add Operation> ex: 1+0=1 the same logic.
                           
//and return Or Opearation result = binary num convert it to decimal. 
int main()
{
    cout<< "Result of Bitwise OR Operator: "<< (12 | 25 );
    cout<<endl;

    return 0;
}